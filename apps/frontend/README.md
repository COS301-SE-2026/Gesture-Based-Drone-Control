frontend runs on port 3000 (vite.config)

yarn dev - developer environment
yarn build - build for production
yarn lint - lint js code
yarn test - runs playwright testing
yarn ui-add {component} - adds shadcn component {component} = shadcn component