Installation instructions

Provide executable permission to uv-install.sh (chmod +x uv-install.sh)
And then execute it 
This is our package manager for Python
It should work on all linux based systems including wsl (arch may be able to get from package managers) (homebrew should have it for mac users)

makefile wraps the uv commands with necessary flags 
starts api on port 3001